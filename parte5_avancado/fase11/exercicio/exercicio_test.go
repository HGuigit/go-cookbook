package main

import "testing"

func TestClassificaNota(t *testing.T) {
	// TODO 1: Crie a tabela (slice de structs) contendo "nomeDoTeste", "input" (float64) e "esperado" (string)

	// TODO 2: Adicione dois cenários na tabela: um passando nota 8.0 ("Aprovado") e outro com 5.0 ("Reprovado")

	// TODO 3: Itere sobre a tabela usando for range
	
		// TODO 4: Dentro do laço, chame t.Run() passando o nome do teste e executando a validação:
		// resultado := ClassificaNota(cenario.input)
		// se resultado for diferente de esperado -> t.Errorf(...)
}
